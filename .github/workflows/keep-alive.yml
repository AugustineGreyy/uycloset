name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 0 * * *'  # every day at midnight UTC
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl your site
        run: curl -s https://your-vercel-or-site-url.com > /dev/null
